# 2026-03-18

- Instantly SuperSearch enrichment request contract was validated from OpenAPI (`https://developer.instantly.ai/api-reference/openapi.json`): for `POST /api/v2/supersearch-enrichment/enrich-leads-from-supersearch`, required top-level fields include `search_filters` and `limit`; enrichment type flag `work_email_enrichment: true` is accepted.
- Root cause of prior 400s: payload shape mismatch (not permissions-only): `search_filters.industry` must be an object (e.g., `{ "include": [...] }`), and enrichment requires an enabled type flag.
- Proven working generation/enrichment flow (HTTP 200) for wedge lead lists:
  - agent list `eb29c24b-2c97-432f-9d54-e7881c5dd678` → enrichment job `b9aea1b5-350b-4ccf-be23-681afffc42b5`
  - brokerage list `8a8019ab-4870-4d71-a9b1-635ade7ff07f` → enrichment job `735906f4-bf56-4140-9887-8902987e0b8a`
  - photographer list `2bb5d0b6-0dd0-4723-97d1-9fd5648391b1` → enrichment job `88923a48-1639-4838-92f6-4311c83d7c00`
- SuperSearch candidate counts at run time:
  - agent: `180469`
  - brokerage: `25383`
  - photographer: `450`
- Operational nuance: immediately after enqueue, `lead-lists` `prospects_count` can remain `0` while `GET /api/v2/supersearch-enrichment/{resource_id}` reports `exists: true` and `in_progress: true`; counts require async processing delay before materialization.
