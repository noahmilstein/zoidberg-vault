# 2026-02-23

## Config changes (completed)
1. **Memory flush** enabled (`compaction.memoryFlush.enabled: true`)
2. **OpenAI embeddings** configured (`text-embedding-3-small` for memory search)
3. **Hybrid search** enabled (vector 0.7 / text 0.3, MMR λ=0.7, temporal decay 30-day half-life)
4. **Session memory indexing** enabled (`experimental.sessionMemory: true`)
5. **Model hierarchy** implemented:
   - Main session: Opus (unchanged)
   - Heartbeats: `openrouter/openai/gpt-5-mini`
   - Sub-agents default: `openrouter/openai/gpt-5-mini`
   - Escalation: pass explicit `model` param on spawn for harder tasks

## Evaluated
- Reviewed `zscole/model-hierarchy-skill` repo (tweet from @0xzak)
- Conclusion: valid concept, thin implementation. Keyword-based routing heuristic is brittle. Self-classification paradox (expensive model runs to decide it shouldn't run). OpenClaw already has native model routing via config + per-spawn overrides. We implemented the same outcome without external dependencies.

## Evaluated
- Reviewed SOAR paper (arxiv 2601.18778) from @rryssf_ tweet — solid research, tweet overhyped it. Meta-RL for self-generated curricula. Nothing actionable for us (it's about training, not inference).
- Reviewed "OpenClaw Token Optimization Guide" by @mattganzak — mostly wrong/fabricated. Bad config syntax, misunderstanding of how OpenClaw injects context, fabricated schema fields. Directional ideas (cheap models for routine work) were valid but we already implemented them properly.

## Slack channel setup
- Created Slack app "Zappa" with Socket Mode
- Connected via appToken + botToken
- Joined all 3 workspace channels: #social, #all-ctrl-alt-agent, #new-channel
- DMs working, groupPolicy set to open
- Slack is now the primary comms layer

## Backup
- Sanitized config backup committed to `openclaw-config-backup.json` (API keys redacted)
- Pushed to GitHub (noahmilstein/zoidberg-vault)

## Slack fixes
- OpenClaw updated 2026.2.21-2 → 2026.2.22-2 (fixed socket-mode crash loop)
- Slack streaming disabled (`streaming: false`) — new version uses Agents & AI Apps API which needs extra Slack app config
- /caa slash command broken (dispatch_failed in Socket Mode) — ticket created, using natural language as workaround

## CAA V2 — All tickets implemented
- 10 tickets created and completed in one session
- 2.1a-d: Cron tracking (schema, API with upsert/soft-delete, Slack failure alerts, UI pages)
- 2.2a-d: Agent activity (schema, API with pagination, Convex crons for rollup + retention, UI pages)
- 2.3a-b: Dashboard (summary cards, queries from dailyStats rollups, recent activity timeline)
- All pushed to master. Needs `npx convex deploy` to create tables + register scheduled functions in prod.
- Sub-agents used Opus 4.6, ~3 min each, total ~82k-106k tokens per agent

## Lessons learned
- Must update CAA tickets at every state change (forgot twice)
- Must chain sub-agent work without stopping — don't wait for human prompt after each completion
- Always use prod API (polished-opossum-635), not dev (majestic-canary-968)
- OpenClaw version should be tracked in TOOLS.md (now 2026.2.22-2)

## Status
- V2 code complete, pending Convex deploy
- Noah AFK, returning with more tasks

## CAA Project Kickoff

- **CtrlAltAgnt (CAA)** — mission control dashboard for OpenClaw agents
- Stack: Next.js 15 + Convex + Tailwind, hosted on Vercel
- Repo: github.com/noahmilstein/ctrlaltagnt (default branch: `master`)
- SSH key: `~/.ssh/id_ed25519_caa` (deploy key for CAA repo)
- Git SSH: `Host github.com-caa` in ssh config
- Convex dev: `majestic-canary-968` / prod: `polished-opossum-635`
- Deploy keys stored in `.env.local` and `.env.production` (gitignored)
- Sub-agents run on Opus 4.6 (changed from gpt-5-mini)
- Noah wants `gpt-5.3-codex` when it gets API access — document everything so he can audit with Cursor
- Spec: `CAA-V0-SPEC.md` in repo — approved by Noah
- Primary branch convention: `master` (never `main`)

## CAA V0 — All Phases Complete
- Phase 1-4 implemented via sub-agents (Opus 4.6), total ~11 min runtime
- Deployed to Vercel: https://ctrlaltagnt.vercel.app/
- Using dev Convex deployment (majestic-canary-968), prod not yet configured
- Vercel build command: `next build` (codegen not needed, _generated files committed)
- Sub-agent announce not delivering to Slack threads — need to investigate
- API key for CAA: stored in .env.local and as Convex env var CAA_API_KEY
- Slack notifications wired up (SLACK_BOT_TOKEN + SLACK_CHANNEL_ID set on Convex dev)

## CAA V1 — Shipped
- Docs restructured: specs/v0-v3, roadmap index
- V1 spec written with mutation attribution, status contract, audit model, Slack sig verification
- V1.1: mutation attribution (actorType, actorId, lastModifiedAt)
- V1.2: audit log, comments, subtasks, tags, due dates
- V1.3: drag-and-drop, filtering, sorting, collapsible done column
- V1.4: richer Slack notifications with task links
- Roadmap and spec status updated — V1 marked ✅ SHIPPED
- Specs exist for v2 and v3 in docs/specs/
