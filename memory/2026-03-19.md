# 2026-03-19

- Re-verified Instantly wedge lead-list status after SuperSearch enrichment runs: `GET /api/v2/supersearch-enrichment/{resource_id}` for all three wedge lists returned `exists: true`, `in_progress: false`, `has_no_leads: false`.
- At the same time, `GET /api/v2/lead-lists` still returned `prospects_count: null` for all wedge list IDs:
  - `eb29c24b-2c97-432f-9d54-e7881c5dd678` (agent)
  - `8a8019ab-4870-4d71-a9b1-635ade7ff07f` (brokerage)
  - `2bb5d0b6-0dd0-4723-97d1-9fd5648391b1` (photographer)
- Durable operational blocker: current accessible API surface does not provide a verifiable per-list populated lead count artifact despite enrichment completion signals; final proof requires either UI evidence (list row counts) or a specific endpoint that returns list lead rows/count.
