## Deploy key: zappa-read-only-simply-sauna-key (read-only)

- Name: zappa-read-only-simply-sauna-key
- Purpose: SSH deploy key for read-only access to Simply Sauna GitHub repositories (git fetch/pull) from the OpenClaw host.
- Location: private key stored on OpenClaw host at /root/.ssh/id_ed25519_simplysauna (private); public key accessible via /root/.ssh/id_ed25519_simplysauna.pub
- Access control: Repo deploy key added with read-only permissions (no write access)
- Rotation/maintenance: consider rotating every 90-180 days; align with broader key-management policies.
- Owner: OpenClaw agent (Zappa)
- Status: recorded for onboarding traceability

## Model Architecture Migration — 2026-02-24 (Zoidberg instance)

- Migrated from single-model `openrouter/openrouter/auto` default to Supervisor / Orchestrator + Workers architecture.
- **Orchestrator (human-facing comms):** `openrouter/openai/gpt-5.2-chat` (must always be used for Slack/webchat comms).
- **Workers (OpenRouter only):**
  - `openrouter/minimax/minimax-m2.5` — drafting / rewriting / research synthesis
  - `openrouter/openai/gpt-5-mini` — summarization / extraction / classification
- Enabled `maxSpawnDepth: 2` for orchestrator pattern.
- Enforced rule: workers never communicate directly with humans; orchestrator assembles final output.
- Root cause of earlier Slack non-response: misconfigured model provider (`openai/gpt-5.2-chat` instead of `openrouter/openai/gpt-5.2-chat`) caused gateway "Unknown model" errors.
- Canonical orchestrator model string going forward: `openrouter/openai/gpt-5.2-chat` (do NOT use direct `openai/*` provider unless separate API auth is installed).
- This applies to the Zoidberg OpenClaw instance only (not Simply Sauna repo config).
