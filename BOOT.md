# BOOT.md — Startup Pointer

On startup, read `BOOTSTRAP.md` first.

Then optionally read:
- `docs/FAILURE_STATES.md`
- latest dated file in `memory/`

`BOOTSTRAP.md` is the authoritative runtime protocol.
